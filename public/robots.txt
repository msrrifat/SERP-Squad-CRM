# Only the marketing homepage is meant for search engines.
# /privacy and /terms stay crawlable (Google's OAuth verification fetches
# them) but carry a robots noindex meta tag; everything else is app UI.
User-agent: *
Allow: /$
Allow: /privacy$
Allow: /terms$
Disallow: /
